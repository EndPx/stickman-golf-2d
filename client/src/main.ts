// Game_Client entry point.
//
// Task 2 established the module. The clock (task 7), Renderer (task 8), Input_Controller (task 9),
// Debug_Overlay (task 10) and the Arena 1 loop (task 11) are wired in here in order.

export function main(): void {
  // Intentionally empty until task 11 wires the client together.
}

main();
