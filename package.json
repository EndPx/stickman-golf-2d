{
  "name": "stickman-golf-2d",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Local single-player 2D mini-golf browser client whose deliverable is a closed verification loop driven by Kane CLI.",
  "scripts": {
    "check": "tsc -p shared/tsconfig.json && tsc -p client/tsconfig.json",
    "dev": "vite client",
    "build": "npm run check && vite build client",
    "preview": "vite preview client"
  },
  "engines": {
    "node": ">=22.18.0"
  },
  "devDependencies": {
    "@types/three": "0.185.4",
    "typescript": "7.0.2",
    "vite": "8.2.2"
  },
  "dependencies": {
    "three": "0.185.1"
  }
}
